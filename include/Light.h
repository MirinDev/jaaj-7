#ifndef LIGHT_H
#define LIGHT_H
/*#include <GameObject.h>
#include <parent.h>

struct Light{
    glm::vec3 pos;
    glm::vec3 dir;
    glm::vec4 color;
    int type;
};

class LightObj:public GameObject{
    public:
        LightObj(Shader *shader, Cam *cam, Light light);
        void render();
        void update(Shader *shader, int i);
        Light light;
    private:
        //
};*/

#endif